import torch

x = torch.rand(5, 3)  # Generate a random vector
print('A random torch tensor : ', x)

print('Check if cuda device is available : ', torch.cuda.is_available())  # Generate a random vector
